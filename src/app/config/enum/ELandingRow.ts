export enum ELandingRowType {
  HEADER = 'header',
  SPECIFICATIONS = 'specifications',
  TEXT = 'text',
}

export enum ELandingRowPosition {
  LEFT = 'left',
  RIGHT = 'right',
  CENTER = 'center',
  COVER = 'cover',
}
